---
const img = "/brand/ZeroMotion-Layerstyle.png";
---
<section id="about" class="section-padding reveal" aria-labelledby="about-title">
  <div class="container-page grid md:grid-cols-2 gap-8 items-stretch">
    <div class="relative rounded-2xl overflow-hidden border border-white/10 min-h-[260px] sm:min-h-[320px] lg:min-h-[420px] flex items-center justify-center" aria-hidden="true">
      <div class="absolute inset-0 bg-no-repeat bg-center bg-contain" style={`background-image:url('${img}'); background-size:contain; background-position:center 60%; background-repeat:no-repeat; background-color:black; will-change:transform;`} id="about-parallax"></div>
      <div class="absolute inset-0 bg-black/40"></div>
    </div>
    <div class="flex flex-col justify-center">
      <h2 id="about-title" class="h2 mb-4">Minimalism. Functionality. Precision.</h2>
      <p class="text-white/85 text-lg mb-4">We believe Houma, Thibodaux, and every town around us are packed with talent, grit, and potential. The only thing missing? A digital presence to match it.</p>
      <p class="text-white/85 text-lg mb-4">We design clean, timeless brands and high-performance web experiences that move with intent — from identity to interface to impression.</p>
      <p class="text-white/80">This is the moment to step forward. To take the skills, the stories, and the hustle of our community and put them where the world can see them. We keep the stack lean, the UX elegant, and the speed uncompromised — so your brand isn’t just seen, it’s remembered.</p>
    </div>
  </div>
</section>
<script>
  const elRef = document.getElementById('about-parallax');
  if(elRef instanceof HTMLElement){
    const target = elRef;
    let ticking = false;
    function onScroll(){
      if(ticking) return; ticking = true;
      requestAnimationFrame(()=>{
        const y = window.scrollY || 0;
        target.style.transform = `translateY(${Math.round(y * -0.05)}px)`;
        ticking = false;
      });
    }
    document.addEventListener('scroll', onScroll, { passive: true });
  }
</script> 